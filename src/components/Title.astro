---
type Props = {
  data: {
    name: string;
    icon: string;
    link: string;
    status: string;
    description: string;
  };
};

const { data } = Astro.props;
---

{
  data && data.status === "enabled" && (
    <div>
      <h1 class="font-oldstandard text-3xl text-blue-900 uppercase m-5">
        {data.name}
      </h1>
    </div>
  )
}
